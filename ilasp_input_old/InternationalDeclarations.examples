#pos(example_0@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_1@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_2@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_3@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_4@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_5@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_6@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_7@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_8@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_9@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_10@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_11@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_12@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,end_trip).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_13@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_14@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_15@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,start_trip).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,end_trip).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_16@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_17@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_18@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_19@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_20@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_21@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_22@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_23@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_24@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_25@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_26@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_27@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_28@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_29@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_30@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_budget_owner).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_rejected_by_administration).
  trace(18,declaration_rejected_by_employee).
  trace(19,declaration_submitted_by_employee).
  trace(20,declaration_approved_by_administration).
  trace(21,declaration_approved_by_budget_owner).
  trace(22,declaration_final_approved_by_supervisor).
  trace(23,request_payment).
  trace(24,payment_handled).
}).
#pos(example_31@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_32@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_33@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,start_trip).
  trace(11,end_trip).
}).
#pos(example_34@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_35@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_36@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_37@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_38@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_39@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_40@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_41@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_42@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_43@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_44@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_45@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,end_trip).
  trace(15,payment_handled).
}).
#pos(example_46@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_47@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_48@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_49@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_50@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_51@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_52@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
}).
#pos(example_53@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_54@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_55@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_56@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_57@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_58@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,declaration_approved_by_administration).
  trace(6,end_trip).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_59@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_60@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_61@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,send_reminder).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_62@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_63@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_64@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_approved_by_budget_owner).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,send_reminder).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_65@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,payment_handled).
}).
#pos(example_66@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_67@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_68@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_69@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_70@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_71@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,start_trip).
  trace(6,declaration_rejected_by_employee).
  trace(7,end_trip).
}).
#pos(example_72@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_73@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_74@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_75@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
}).
#pos(example_76@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,send_reminder).
  trace(12,send_reminder).
}).
#pos(example_77@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_78@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_79@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_80@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_81@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_82@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_rejected_by_administration).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,end_trip).
}).
#pos(example_83@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_84@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_85@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_86@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,end_trip).
}).
#pos(example_87@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
  trace(11,end_trip).
}).
#pos(example_88@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,start_trip).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_89@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_final_approved_by_supervisor).
  trace(2,request_payment).
  trace(3,payment_handled).
  trace(4,start_trip).
  trace(5,end_trip).
}).
#pos(example_90@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_pre_approver).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
  trace(8,start_trip).
  trace(9,end_trip).
}).
#pos(example_91@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_pre_approver).
  trace(2,declaration_rejected_by_supervisor).
  trace(3,declaration_rejected_by_employee).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_92@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_supervisor).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_supervisor).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,start_trip).
  trace(12,end_trip).
}).
#pos(example_93@0, {recognized}, {}, {
  trace(0,declaration_saved_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
}).
#pos(example_94@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_final_approved_by_supervisor).
  trace(2,declaration_rejected_by_missing).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,declaration_rejected_by_missing).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,start_trip).
  trace(11,end_trip).
}).
#pos(example_95@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_pre_approver).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,request_payment).
  trace(4,payment_handled).
  trace(5,start_trip).
  trace(6,end_trip).
}).
#pos(example_96@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_supervisor).
  trace(2,declaration_final_approved_by_director).
  trace(3,request_payment).
  trace(4,payment_handled).
  trace(5,start_trip).
  trace(6,end_trip).
}).
#pos(example_97@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_98@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_99@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_supervisor).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,request_payment).
  trace(6,payment_handled).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_100@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_pre_approver).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,declaration_rejected_by_missing).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_101@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_final_approved_by_supervisor).
  trace(2,declaration_rejected_by_missing).
  trace(3,start_trip).
  trace(4,end_trip).
}).
#pos(example_102@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_103@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_pre_approver).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_pre_approver).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,start_trip).
  trace(13,end_trip).
}).
#pos(example_104@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_supervisor).
  trace(2,declaration_final_approved_by_director).
  trace(3,declaration_rejected_by_missing).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_supervisor).
  trace(6,declaration_final_approved_by_director).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_105@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_106@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_pre_approver).
  trace(2,declaration_approved_by_supervisor).
  trace(3,declaration_final_approved_by_director).
  trace(4,request_payment).
  trace(5,payment_handled).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_107@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_final_approved_by_supervisor).
  trace(2,declaration_rejected_by_missing).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,request_payment).
  trace(6,payment_handled).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_108@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,start_trip).
  trace(4,end_trip).
}).
#pos(example_109@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,request_payment).
  trace(4,payment_handled).
  trace(5,start_trip).
  trace(6,end_trip).
}).
#pos(example_110@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_approved_by_budget_owner).
  trace(3,declaration_final_approved_by_supervisor).
  trace(4,request_payment).
  trace(5,payment_handled).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_111@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
  trace(8,start_trip).
  trace(9,end_trip).
}).
#pos(example_112@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_approved_by_supervisor).
  trace(3,declaration_final_approved_by_director).
  trace(4,request_payment).
  trace(5,payment_handled).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_113@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_rejected_by_supervisor).
  trace(3,declaration_rejected_by_employee).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_114@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_approved_by_budget_owner).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_115@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_116@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_117@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
  trace(15,start_trip).
  trace(16,end_trip).
}).
#pos(example_118@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_rejected_by_employee).
}).
#pos(example_119@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_approved_by_budget_owner).
  trace(3,declaration_rejected_by_supervisor).
  trace(4,declaration_rejected_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
}).
#pos(example_120@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,request_payment).
  trace(5,payment_handled).
  trace(6,end_trip).
}).
#pos(example_121@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_approved_by_administration).
  trace(4,declaration_approved_by_budget_owner).
  trace(5,declaration_rejected_by_supervisor).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_122@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,payment_handled).
  trace(6,request_payment).
}).
#pos(example_123@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,start_trip).
  trace(13,end_trip).
}).
#pos(example_124@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_approved_by_supervisor).
  trace(6,declaration_final_approved_by_director).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_125@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_approved_by_budget_owner).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,start_trip).
  trace(11,end_trip).
}).
#pos(example_126@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_approved_by_budget_owner).
  trace(3,declaration_approved_by_supervisor).
  trace(4,declaration_final_approved_by_director).
  trace(5,request_payment).
  trace(6,payment_handled).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_127@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_approved_by_administration).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,request_payment).
  trace(6,payment_handled).
}).
#pos(example_128@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_approved_by_administration).
  trace(4,declaration_approved_by_budget_owner).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_129@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,request_payment).
  trace(6,payment_handled).
}).
#pos(example_130@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,end_trip).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_131@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,start_trip).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_132@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_rejected_by_employee).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_133@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_rejected_by_administration).
  trace(4,declaration_rejected_by_employee).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_134@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_rejected_by_administration).
  trace(4,declaration_rejected_by_employee).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_135@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_rejected_by_administration).
  trace(4,declaration_rejected_by_employee).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_136@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,start_trip).
  trace(2,declaration_rejected_by_administration).
  trace(3,declaration_rejected_by_employee).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_137@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_138@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,start_trip).
  trace(3,declaration_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_139@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_rejected_by_administration).
  trace(4,declaration_rejected_by_employee).
}).
#pos(example_140@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_saved_by_employee).
}).
#pos(example_141@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_approved_by_budget_owner).
  trace(3,declaration_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,request_payment).
  trace(6,payment_handled).
  trace(7,end_trip).
}).
#pos(example_142@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,start_trip).
  trace(3,declaration_rejected_by_employee).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,end_trip).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_143@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,declaration_submitted_by_employee).
  trace(2,declaration_rejected_by_administration).
  trace(3,declaration_rejected_by_employee).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_144@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,start_trip).
  trace(12,end_trip).
}).
#pos(example_145@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_rejected_by_administration).
  trace(2,declaration_rejected_by_employee).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
  trace(13,start_trip).
  trace(14,end_trip).
}).
#pos(example_146@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,declaration_approved_by_administration).
  trace(2,declaration_final_approved_by_supervisor).
  trace(3,request_payment).
  trace(4,start_trip).
  trace(5,payment_handled).
  trace(6,end_trip).
}).
#pos(example_147@0, {recognized}, {}, {
  trace(0,declaration_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,declaration_approved_by_administration).
  trace(4,declaration_rejected_by_budget_owner).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_148@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_rejected_by_administration).
  trace(4,declaration_rejected_by_employee).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_149@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_150@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_151@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_152@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_153@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_154@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_155@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_156@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_157@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
}).
#pos(example_158@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_159@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_160@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_161@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_162@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_163@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_164@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_saved_by_employee).
}).
#pos(example_165@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_166@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_167@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_168@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_169@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_170@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,declaration_saved_by_employee).
}).
#pos(example_171@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_172@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_173@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_174@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
}).
#pos(example_175@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_176@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,payment_handled).
  trace(12,request_payment).
}).
#pos(example_177@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_rejected_by_administration).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,start_trip).
  trace(12,end_trip).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_rejected_by_supervisor).
  trace(20,declaration_rejected_by_employee).
  trace(21,declaration_submitted_by_employee).
  trace(22,declaration_approved_by_administration).
  trace(23,declaration_approved_by_budget_owner).
  trace(24,declaration_final_approved_by_supervisor).
  trace(25,request_payment).
  trace(26,payment_handled).
}).
#pos(example_178@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_179@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_180@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_181@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,send_reminder).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_182@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,end_trip).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_183@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_184@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_budget_owner).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_185@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_186@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_187@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_188@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_189@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_190@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,end_trip).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_191@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_192@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_approved_by_administration).
  trace(20,declaration_approved_by_budget_owner).
  trace(21,declaration_final_approved_by_supervisor).
  trace(22,request_payment).
  trace(23,payment_handled).
}).
#pos(example_193@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_194@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_195@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_rejected_by_administration).
  trace(9,permit_rejected_by_employee).
  trace(10,permit_submitted_by_employee).
  trace(11,permit_rejected_by_administration).
  trace(12,permit_rejected_by_employee).
  trace(13,permit_submitted_by_employee).
  trace(14,permit_approved_by_administration).
  trace(15,permit_approved_by_budget_owner).
  trace(16,permit_final_approved_by_supervisor).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_196@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_rejected_by_administration).
  trace(9,permit_rejected_by_employee).
  trace(10,permit_submitted_by_employee).
  trace(11,permit_approved_by_administration).
  trace(12,permit_approved_by_budget_owner).
  trace(13,permit_final_approved_by_supervisor).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_197@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,end_trip).
}).
#pos(example_198@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_199@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_200@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_201@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_budget_owner).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_rejected_by_administration).
  trace(8,permit_rejected_by_employee).
  trace(9,permit_submitted_by_employee).
  trace(10,permit_approved_by_administration).
  trace(11,permit_approved_by_budget_owner).
  trace(12,permit_final_approved_by_supervisor).
  trace(13,send_reminder).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_202@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_rejected_by_administration).
  trace(6,start_trip).
  trace(7,permit_rejected_by_employee).
  trace(8,permit_submitted_by_employee).
  trace(9,permit_approved_by_administration).
  trace(10,permit_approved_by_budget_owner).
  trace(11,end_trip).
  trace(12,permit_final_approved_by_supervisor).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_203@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
  trace(8,start_trip).
  trace(9,end_trip).
}).
#pos(example_204@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_205@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_206@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,end_trip).
}).
#pos(example_207@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_208@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,start_trip).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_209@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_210@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_rejected_by_supervisor).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_211@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,send_reminder).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_212@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,send_reminder).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_approved_by_budget_owner).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_213@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,send_reminder).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_approved_by_administration).
  trace(20,declaration_approved_by_budget_owner).
  trace(21,declaration_final_approved_by_supervisor).
  trace(22,request_payment).
  trace(23,payment_handled).
}).
#pos(example_214@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_215@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_216@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_saved_by_employee).
}).
#pos(example_217@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_218@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_rejected_by_budget_owner).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_219@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,declaration_submitted_by_employee).
  trace(9,start_trip).
  trace(10,declaration_approved_by_administration).
  trace(11,end_trip).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_220@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_221@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_222@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_budget_owner).
  trace(8,end_trip).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_223@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,start_trip).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,end_trip).
  trace(14,payment_handled).
}).
#pos(example_224@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_225@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,permit_final_approved_by_director).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_226@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,send_reminder).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_227@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_saved_by_employee).
}).
#pos(example_228@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_229@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_230@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_231@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_232@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_233@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_234@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_supervisor).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_supervisor).
  trace(16,declaration_final_approved_by_director).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_235@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_saved_by_employee).
  trace(7,end_trip).
}).
#pos(example_236@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_237@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_238@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_239@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_240@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,end_trip).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_241@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_242@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_243@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_244@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_245@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_246@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_rejected_by_budget_owner).
  trace(13,declaration_rejected_by_employee).
}).
#pos(example_247@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,end_trip).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_248@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_249@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_250@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_251@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_252@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_253@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_254@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_budget_owner).
  trace(12,declaration_rejected_by_employee).
}).
#pos(example_255@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_256@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,start_trip).
  trace(12,end_trip).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_257@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_258@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,start_trip).
  trace(9,end_trip).
}).
#pos(example_259@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_260@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,start_trip).
  trace(12,end_trip).
}).
#pos(example_261@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_262@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
}).
#pos(example_263@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_264@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_265@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,end_trip).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_266@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_saved_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
}).
#pos(example_267@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_budget_owner).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_268@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,end_trip).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_269@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
  trace(12,end_trip).
}).
#pos(example_270@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_271@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_272@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,end_trip).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_273@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_274@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_275@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_276@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_277@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_278@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_279@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_approved_by_supervisor).
  trace(18,declaration_rejected_by_director).
  trace(19,declaration_rejected_by_employee).
  trace(20,declaration_submitted_by_employee).
  trace(21,declaration_approved_by_administration).
  trace(22,declaration_final_approved_by_supervisor).
  trace(23,request_payment).
  trace(24,payment_handled).
}).
#pos(example_280@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_281@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,start_trip).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_282@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_283@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_284@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_rejected_by_budget_owner).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_285@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_286@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
}).
#pos(example_287@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_288@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,end_trip).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_289@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_290@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,start_trip).
  trace(8,declaration_rejected_by_employee).
  trace(9,end_trip).
}).
#pos(example_291@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_292@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,start_trip).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_293@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_294@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_295@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_296@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_297@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_298@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_299@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_300@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_301@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_302@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_303@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,send_reminder).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_304@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_budget_owner).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_305@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_306@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_307@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_308@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_309@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_310@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_311@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_approved_by_supervisor).
  trace(17,declaration_final_approved_by_director).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_312@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,end_trip).
  trace(9,payment_handled).
}).
#pos(example_313@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_314@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,start_trip).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_315@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
}).
#pos(example_316@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_317@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_318@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_319@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_supervisor).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_320@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,end_trip).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_321@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_322@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_323@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_324@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,end_trip).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_325@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,end_trip).
}).
#pos(example_326@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_327@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_328@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_rejected_by_supervisor).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_329@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,end_trip).
}).
#pos(example_330@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_331@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_332@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,send_reminder).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_333@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_334@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_335@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
}).
#pos(example_336@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,send_reminder).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_337@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,send_reminder).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_338@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,send_reminder).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_339@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,start_trip).
  trace(10,end_trip).
}).
#pos(example_340@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_341@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_rejected_by_administration).
  trace(20,declaration_rejected_by_employee).
  trace(21,declaration_submitted_by_employee).
  trace(22,declaration_approved_by_administration).
  trace(23,declaration_final_approved_by_supervisor).
  trace(24,request_payment).
  trace(25,payment_handled).
}).
#pos(example_342@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_approved_by_budget_owner).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_343@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_budget_owner).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_approved_by_budget_owner).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,send_reminder).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_344@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_345@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_rejected_by_budget_owner).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_approved_by_budget_owner).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,send_reminder).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_346@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_347@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,send_reminder).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_348@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_director).
  trace(4,permit_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,end_trip).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_349@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_350@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_351@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_352@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_approved_by_administration).
  trace(20,declaration_approved_by_supervisor).
  trace(21,declaration_final_approved_by_director).
  trace(22,request_payment).
  trace(23,payment_handled).
}).
#pos(example_353@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_354@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_355@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,end_trip).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_356@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_357@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_358@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_359@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,payment_handled).
}).
#pos(example_360@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_361@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_362@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_approved_by_supervisor).
  trace(16,declaration_final_approved_by_director).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_363@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,end_trip).
}).
#pos(example_364@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_365@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_366@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_367@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,send_reminder).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_368@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_369@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_370@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_371@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,end_trip).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_372@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_373@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,end_trip).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_374@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_375@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_376@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_377@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,declaration_submitted_by_employee).
  trace(10,start_trip).
  trace(11,end_trip).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_378@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_379@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_380@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_381@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_approved_by_budget_owner).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_382@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,send_reminder).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_rejected_by_budget_owner).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_383@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
  trace(14,end_trip).
}).
#pos(example_384@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_385@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_386@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,end_trip).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_387@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_388@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,end_trip).
  trace(11,payment_handled).
}).
#pos(example_389@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,start_trip).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,end_trip).
  trace(12,payment_handled).
}).
#pos(example_390@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
}).
#pos(example_391@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_rejected_by_budget_owner).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_rejected_by_administration).
  trace(19,declaration_rejected_by_employee).
  trace(20,declaration_submitted_by_employee).
  trace(21,declaration_approved_by_administration).
  trace(22,declaration_final_approved_by_supervisor).
  trace(23,request_payment).
  trace(24,payment_handled).
}).
#pos(example_392@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_393@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_394@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_director).
  trace(4,permit_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_395@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_396@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_397@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_398@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_rejected_by_supervisor).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_rejected_by_administration).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_399@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_400@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_401@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_rejected_by_budget_owner).
  trace(7,permit_rejected_by_employee).
  trace(8,permit_submitted_by_employee).
  trace(9,permit_approved_by_administration).
  trace(10,permit_approved_by_budget_owner).
  trace(11,permit_final_approved_by_supervisor).
  trace(12,start_trip).
  trace(13,end_trip).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_402@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_403@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_approved_by_budget_owner).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_404@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_405@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_rejected_by_administration).
  trace(7,permit_rejected_by_employee).
  trace(8,permit_submitted_by_employee).
  trace(9,permit_approved_by_administration).
  trace(10,permit_approved_by_budget_owner).
  trace(11,permit_final_approved_by_supervisor).
  trace(12,start_trip).
  trace(13,end_trip).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_rejected_by_budget_owner).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_approved_by_administration).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_406@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,end_trip).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_407@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_408@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
  trace(14,end_trip).
}).
#pos(example_409@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_rejected_by_supervisor).
  trace(16,declaration_rejected_by_employee).
}).
#pos(example_410@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,end_trip).
  trace(9,payment_handled).
}).
#pos(example_411@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_rejected_by_supervisor).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_412@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_413@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_414@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_415@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,end_trip).
}).
#pos(example_416@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,end_trip).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_417@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_418@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_419@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_420@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_421@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_422@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,send_reminder).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_423@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_424@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,send_reminder).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_425@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_426@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,start_trip).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_427@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_428@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_429@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_rejected_by_administration).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,end_trip).
  trace(14,payment_handled).
}).
#pos(example_430@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_431@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_approved_by_budget_owner).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_432@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_433@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_434@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_435@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_436@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_437@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,end_trip).
}).
#pos(example_438@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,end_trip).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_439@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_440@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_rejected_by_administration).
  trace(3,permit_rejected_by_employee).
  trace(4,end_trip).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_rejected_by_administration).
  trace(7,permit_rejected_by_employee).
  trace(8,permit_submitted_by_employee).
  trace(9,permit_approved_by_administration).
  trace(10,permit_approved_by_budget_owner).
  trace(11,permit_final_approved_by_supervisor).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_rejected_by_supervisor).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_441@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_442@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_rejected_by_supervisor).
  trace(4,end_trip).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_443@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,send_reminder).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_supervisor).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_rejected_by_supervisor).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_444@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_445@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_446@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_447@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_448@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_449@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_450@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_451@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_452@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_453@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_454@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_455@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,start_trip).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_456@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,end_trip).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_approved_by_supervisor).
  trace(14,declaration_final_approved_by_director).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_457@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,send_reminder).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_458@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_459@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_460@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_461@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_462@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_463@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_464@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,end_trip).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_465@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_466@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,start_trip).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_467@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_468@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
}).
#pos(example_469@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,start_trip).
  trace(8,permit_approved_by_supervisor).
  trace(9,permit_final_approved_by_director).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_470@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_471@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,start_trip).
  trace(12,end_trip).
}).
#pos(example_472@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_473@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,end_trip).
}).
#pos(example_474@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_475@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_476@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_477@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_478@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_rejected_by_budget_owner).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_approved_by_supervisor).
  trace(18,declaration_final_approved_by_director).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_479@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_approved_by_supervisor).
  trace(17,declaration_final_approved_by_director).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_480@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_rejected_by_budget_owner).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
  trace(15,end_trip).
}).
#pos(example_481@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_482@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_483@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,end_trip).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_484@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_485@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_486@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,permit_rejected_by_missing).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_487@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
  trace(15,end_trip).
}).
#pos(example_488@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_489@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_rejected_by_budget_owner).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_administration).
  trace(6,permit_approved_by_budget_owner).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_approved_by_budget_owner).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_490@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_rejected_by_supervisor).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_491@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
  trace(13,send_reminder).
  trace(14,send_reminder).
}).
#pos(example_492@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
  trace(14,end_trip).
}).
#pos(example_493@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_rejected_by_supervisor).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_administration).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,send_reminder).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_rejected_by_administration).
  trace(18,declaration_rejected_by_employee).
  trace(19,declaration_submitted_by_employee).
  trace(20,declaration_approved_by_administration).
  trace(21,declaration_final_approved_by_supervisor).
  trace(22,request_payment).
  trace(23,payment_handled).
}).
#pos(example_494@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,end_trip).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_495@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_final_approved_by_supervisor).
  trace(17,request_payment).
  trace(18,payment_handled).
}).
#pos(example_496@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,declaration_rejected_by_administration).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_497@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_approved_by_budget_owner).
  trace(8,permit_approved_by_supervisor).
  trace(9,permit_rejected_by_director).
  trace(10,permit_rejected_by_employee).
  trace(11,permit_submitted_by_employee).
  trace(12,permit_approved_by_administration).
  trace(13,permit_approved_by_budget_owner).
  trace(14,permit_approved_by_supervisor).
  trace(15,permit_final_approved_by_director).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_final_approved_by_supervisor).
  trace(20,request_payment).
  trace(21,payment_handled).
}).
#pos(example_498@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_499@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_budget_owner).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
  trace(16,end_trip).
}).
#pos(example_500@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
  trace(10,end_trip).
}).
#pos(example_501@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
  trace(16,end_trip).
}).
#pos(example_502@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,end_trip).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_503@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_504@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_505@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,permit_approved_by_budget_owner).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_rejected_by_supervisor).
  trace(16,declaration_rejected_by_employee).
  trace(17,declaration_submitted_by_employee).
  trace(18,declaration_approved_by_administration).
  trace(19,declaration_approved_by_budget_owner).
  trace(20,declaration_final_approved_by_supervisor).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_506@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_507@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_508@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_rejected_by_supervisor).
  trace(10,declaration_rejected_by_employee).
}).
#pos(example_509@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_administration).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_administration).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_510@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,payment_handled).
}).
#pos(example_511@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_512@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_513@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_514@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_515@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_516@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,end_trip).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_517@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_rejected_by_supervisor).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_518@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_approved_by_budget_owner).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_519@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,send_reminder).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_520@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_521@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
}).
#pos(example_522@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_saved_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_523@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_rejected_by_budget_owner).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_524@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_525@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_526@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_saved_by_employee).
  trace(5,end_trip).
}).
#pos(example_527@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_528@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_approved_by_budget_owner).
  trace(4,end_trip).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_529@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_budget_owner).
  trace(5,permit_rejected_by_supervisor).
  trace(6,permit_rejected_by_employee).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_administration).
  trace(9,permit_approved_by_budget_owner).
  trace(10,permit_final_approved_by_supervisor).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_530@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_531@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_rejected_by_supervisor).
  trace(3,end_trip).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_rejected_by_budget_owner).
  trace(8,permit_rejected_by_employee).
  trace(9,permit_submitted_by_employee).
  trace(10,permit_approved_by_administration).
  trace(11,permit_approved_by_budget_owner).
  trace(12,permit_final_approved_by_supervisor).
  trace(13,send_reminder).
  trace(14,send_reminder).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_rejected_by_administration).
  trace(20,declaration_rejected_by_employee).
  trace(21,declaration_submitted_by_employee).
  trace(22,declaration_approved_by_administration).
  trace(23,declaration_approved_by_budget_owner).
  trace(24,declaration_final_approved_by_supervisor).
  trace(25,request_payment).
  trace(26,payment_handled).
}).
#pos(example_532@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_533@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_rejected_by_budget_owner).
  trace(8,permit_rejected_by_employee).
  trace(9,permit_submitted_by_employee).
  trace(10,permit_approved_by_administration).
  trace(11,permit_approved_by_budget_owner).
  trace(12,permit_final_approved_by_supervisor).
  trace(13,send_reminder).
  trace(14,send_reminder).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_rejected_by_administration).
  trace(17,declaration_rejected_by_employee).
  trace(18,declaration_submitted_by_employee).
  trace(19,declaration_rejected_by_administration).
  trace(20,declaration_rejected_by_employee).
  trace(21,declaration_submitted_by_employee).
  trace(22,declaration_approved_by_administration).
  trace(23,declaration_approved_by_budget_owner).
  trace(24,declaration_final_approved_by_supervisor).
  trace(25,request_payment).
  trace(26,payment_handled).
}).
#pos(example_534@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_rejected_by_supervisor).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,payment_handled).
}).
#pos(example_535@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_rejected_by_budget_owner).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_536@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,start_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_administration).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_rejected_by_administration).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_administration).
  trace(17,declaration_approved_by_budget_owner).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_537@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,end_trip).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_rejected_by_administration).
  trace(15,declaration_rejected_by_employee).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_administration).
  trace(18,declaration_approved_by_budget_owner).
  trace(19,declaration_approved_by_supervisor).
  trace(20,declaration_final_approved_by_director).
  trace(21,request_payment).
  trace(22,payment_handled).
}).
#pos(example_538@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_administration).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,start_trip).
  trace(11,payment_handled).
  trace(12,end_trip).
}).
#pos(example_539@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_rejected_by_supervisor).
  trace(4,start_trip).
  trace(5,permit_rejected_by_employee).
  trace(6,end_trip).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_rejected_by_administration).
  trace(9,permit_rejected_by_employee).
  trace(10,permit_submitted_by_employee).
  trace(11,permit_approved_by_administration).
  trace(12,permit_approved_by_budget_owner).
  trace(13,permit_final_approved_by_supervisor).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_rejected_by_supervisor).
  trace(18,declaration_rejected_by_employee).
  trace(19,declaration_submitted_by_employee).
  trace(20,declaration_approved_by_administration).
  trace(21,declaration_approved_by_budget_owner).
  trace(22,declaration_final_approved_by_supervisor).
  trace(23,request_payment).
  trace(24,payment_handled).
}).
#pos(example_540@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_administration).
  trace(13,declaration_approved_by_budget_owner).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_541@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_budget_owner).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_542@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_approved_by_budget_owner).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,end_trip).
  trace(7,send_reminder).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_543@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_544@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_545@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_pre_approver).
  trace(5,declaration_rejected_by_employee).
  trace(6,start_trip).
  trace(7,end_trip).
}).
#pos(example_546@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_547@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_548@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_549@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
}).
#pos(example_550@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_551@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,end_trip).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_552@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_pre_approver).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_553@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,declaration_submitted_by_employee).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_554@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_555@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_556@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_pre_approver).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_pre_approver).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_pre_approver).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_557@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_558@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_559@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,request_payment).
  trace(6,end_trip).
  trace(7,payment_handled).
}).
#pos(example_560@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_561@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_562@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_563@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_saved_by_employee).
}).
#pos(example_564@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_565@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,declaration_rejected_by_missing).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_566@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_567@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_568@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_supervisor).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_569@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_570@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_571@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_rejected_by_missing).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_pre_approver).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_572@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,permit_rejected_by_missing).
  trace(7,permit_submitted_by_employee).
  trace(8,permit_approved_by_pre_approver).
  trace(9,permit_final_approved_by_supervisor).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_573@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_574@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,permit_rejected_by_missing).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_575@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,permit_rejected_by_missing).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_pre_approver).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_576@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_pre_approver).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_pre_approver).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_577@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_administration).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_saved_by_employee).
}).
#pos(example_578@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_579@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_580@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_581@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_582@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_rejected_by_director).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_supervisor).
  trace(15,declaration_final_approved_by_director).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_583@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,permit_rejected_by_missing).
  trace(4,start_trip).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_pre_approver).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_584@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,declaration_rejected_by_missing).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_pre_approver).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_585@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_rejected_by_missing).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_pre_approver).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_586@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_saved_by_employee).
  trace(6,permit_rejected_by_missing).
}).
#pos(example_587@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,permit_approved_by_pre_approver).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_588@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_rejected_by_supervisor).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_pre_approver).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_589@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_590@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_591@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_592@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_593@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_pre_approver).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,declaration_rejected_by_missing).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_final_approved_by_director).
  trace(15,declaration_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_594@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,end_trip).
  trace(9,payment_handled).
}).
#pos(example_595@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_596@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_final_approved_by_supervisor).
  trace(4,request_payment).
  trace(5,start_trip).
  trace(6,payment_handled).
  trace(7,end_trip).
}).
#pos(example_597@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_598@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_supervisor).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_599@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_rejected_by_director).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_rejected_by_supervisor).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_pre_approver).
  trace(17,declaration_approved_by_supervisor).
  trace(18,declaration_final_approved_by_director).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_600@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_601@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_602@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_pre_approver).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_603@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_604@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,permit_approved_by_supervisor).
  trace(7,permit_final_approved_by_director).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_pre_approver).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_605@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_606@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_607@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_608@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_609@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_610@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_611@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,declaration_rejected_by_missing).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_pre_approver).
  trace(12,declaration_approved_by_supervisor).
  trace(13,declaration_final_approved_by_director).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_612@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_613@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_614@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_supervisor).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_615@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_pre_approver).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_pre_approver).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_616@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_617@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_618@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,declaration_rejected_by_missing).
  trace(8,end_trip).
  trace(9,permit_rejected_by_missing).
}).
#pos(example_619@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,permit_rejected_by_missing).
}).
#pos(example_620@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_621@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_622@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,declaration_rejected_by_missing).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_623@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_624@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_rejected_by_missing).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_pre_approver).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_625@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_pre_approver).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_pre_approver).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_626@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_627@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_628@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_629@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,declaration_rejected_by_pre_approver).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_630@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_rejected_by_pre_approver).
  trace(5,permit_rejected_by_employee).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_pre_approver).
  trace(8,permit_final_approved_by_supervisor).
  trace(9,start_trip).
  trace(10,end_trip).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_631@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_632@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_633@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_634@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_635@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_636@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_637@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,declaration_rejected_by_missing).
}).
#pos(example_638@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_pre_approver).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_pre_approver).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_639@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,send_reminder).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_640@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_rejected_by_director).
  trace(9,declaration_rejected_by_employee).
}).
#pos(example_641@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,send_reminder).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_approved_by_budget_owner).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_642@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,declaration_submitted_by_employee).
  trace(4,end_trip).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_643@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_644@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,declaration_rejected_by_supervisor).
  trace(6,end_trip).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_645@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_supervisor).
  trace(6,declaration_final_approved_by_director).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_646@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_647@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,declaration_submitted_by_employee).
  trace(3,declaration_approved_by_administration).
  trace(4,declaration_final_approved_by_supervisor).
  trace(5,request_payment).
  trace(6,payment_handled).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_648@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_649@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_650@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_651@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,permit_rejected_by_missing).
  trace(10,payment_handled).
}).
#pos(example_652@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_653@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_654@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_655@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_administration).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_656@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,end_trip).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_657@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,send_reminder).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_658@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_supervisor).
  trace(8,declaration_final_approved_by_director).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_659@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_660@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_supervisor).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_supervisor).
  trace(10,declaration_final_approved_by_director).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_661@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_662@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
}).
#pos(example_663@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,permit_rejected_by_missing).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_664@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_665@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,permit_rejected_by_missing).
  trace(8,payment_handled).
}).
#pos(example_666@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_667@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_rejected_by_supervisor).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_668@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,end_trip).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_669@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_director).
  trace(3,permit_approved_by_supervisor).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_administration).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_670@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_671@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,permit_rejected_by_missing).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_approved_by_supervisor).
  trace(8,permit_final_approved_by_director).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_672@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_673@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_pre_approver).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_674@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_675@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_676@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,permit_rejected_by_missing).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_677@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,permit_rejected_by_missing).
  trace(8,payment_handled).
}).
#pos(example_678@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_final_approved_by_director).
  trace(9,declaration_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_679@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,declaration_rejected_by_missing).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_680@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,permit_rejected_by_missing).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_681@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
}).
#pos(example_682@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_rejected_by_budget_owner).
  trace(8,declaration_rejected_by_employee).
}).
#pos(example_683@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_approved_by_budget_owner).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_684@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
  trace(8,end_trip).
}).
#pos(example_685@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
}).
#pos(example_686@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
}).
#pos(example_687@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_688@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_689@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_690@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,declaration_rejected_by_missing).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_691@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_approved_by_pre_approver).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_pre_approver).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,end_trip).
  trace(12,payment_handled).
}).
#pos(example_692@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,start_trip).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_693@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_pre_approver).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_694@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_695@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_696@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_697@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
}).
#pos(example_698@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_699@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_director).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_700@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_701@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_rejected_by_supervisor).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_pre_approver).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_702@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_rejected_by_supervisor).
  trace(5,declaration_rejected_by_employee).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,start_trip).
  trace(10,payment_handled).
  trace(11,end_trip).
}).
#pos(example_703@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_supervisor).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,declaration_rejected_by_missing).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_704@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_705@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_administration).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_706@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_pre_approver).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_pre_approver).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_707@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,declaration_rejected_by_missing).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_pre_approver).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_708@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_pre_approver).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_pre_approver).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_approved_by_pre_approver).
  trace(13,declaration_rejected_by_supervisor).
  trace(14,declaration_rejected_by_employee).
  trace(15,declaration_submitted_by_employee).
  trace(16,declaration_approved_by_pre_approver).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_709@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,payment_handled).
  trace(8,start_trip).
  trace(9,end_trip).
}).
#pos(example_710@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,permit_rejected_by_missing).
}).
#pos(example_711@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_pre_approver).
  trace(2,permit_rejected_by_employee).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_pre_approver).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_712@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,permit_submitted_by_employee).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_supervisor).
  trace(6,declaration_final_approved_by_director).
  trace(7,request_payment).
  trace(8,payment_handled).
  trace(9,end_trip).
}).
#pos(example_713@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,end_trip).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,declaration_rejected_by_missing).
  trace(16,declaration_submitted_by_employee).
  trace(17,declaration_approved_by_pre_approver).
  trace(18,declaration_final_approved_by_supervisor).
  trace(19,request_payment).
  trace(20,payment_handled).
}).
#pos(example_714@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_supervisor).
  trace(11,declaration_final_approved_by_director).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_715@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,permit_rejected_by_missing).
  trace(3,permit_submitted_by_employee).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,permit_rejected_by_missing).
  trace(6,permit_submitted_by_employee).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,start_trip).
  trace(9,end_trip).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_716@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_pre_approver).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_717@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,declaration_rejected_by_missing).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_718@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_administration).
  trace(5,end_trip).
  trace(6,declaration_approved_by_budget_owner).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_719@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
}).
#pos(example_720@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_administration).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,request_payment).
  trace(8,payment_handled).
}).
#pos(example_721@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_approved_by_pre_approver).
  trace(8,declaration_approved_by_supervisor).
  trace(9,declaration_final_approved_by_director).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_722@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_saved_by_employee).
  trace(5,permit_rejected_by_missing).
}).
#pos(example_723@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,send_reminder).
  trace(5,declaration_saved_by_employee).
}).
#pos(example_724@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_725@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_rejected_by_supervisor).
  trace(3,permit_rejected_by_employee).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
}).
#pos(example_726@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,declaration_submitted_by_employee).
  trace(3,start_trip).
  trace(4,declaration_rejected_by_supervisor).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_727@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_pre_approver).
  trace(6,start_trip).
  trace(7,permit_final_approved_by_supervisor).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_pre_approver).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_728@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,start_trip).
  trace(5,end_trip).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_pre_approver).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,declaration_rejected_by_missing).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_pre_approver).
  trace(14,declaration_final_approved_by_supervisor).
  trace(15,request_payment).
  trace(16,payment_handled).
}).
#pos(example_729@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_730@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_approved_by_pre_approver).
  trace(6,end_trip).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_731@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_final_approved_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_rejected_by_supervisor).
  trace(6,declaration_rejected_by_employee).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_approved_by_administration).
  trace(9,declaration_approved_by_budget_owner).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_732@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,start_trip).
  trace(6,declaration_final_approved_by_supervisor).
  trace(7,end_trip).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_733@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,start_trip).
  trace(4,declaration_submitted_by_employee).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,request_payment).
  trace(7,end_trip).
  trace(8,payment_handled).
}).
#pos(example_734@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_rejected_by_pre_approver).
  trace(11,declaration_rejected_by_employee).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_final_approved_by_supervisor).
  trace(14,request_payment).
  trace(15,payment_handled).
}).
#pos(example_735@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,send_reminder).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_approved_by_administration).
  trace(12,declaration_final_approved_by_supervisor).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_736@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_approved_by_supervisor).
  trace(6,permit_final_approved_by_director).
  trace(7,start_trip).
  trace(8,end_trip).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_final_approved_by_supervisor).
  trace(11,request_payment).
  trace(12,payment_handled).
}).
#pos(example_737@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_supervisor).
  trace(2,permit_final_approved_by_director).
  trace(3,permit_rejected_by_missing).
  trace(4,permit_submitted_by_employee).
  trace(5,permit_final_approved_by_supervisor).
  trace(6,start_trip).
  trace(7,end_trip).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_738@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_pre_approver).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_final_approved_by_supervisor).
  trace(10,request_payment).
  trace(11,payment_handled).
}).
#pos(example_739@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,declaration_submitted_by_employee).
  trace(4,declaration_approved_by_pre_approver).
  trace(5,declaration_final_approved_by_supervisor).
  trace(6,declaration_rejected_by_missing).
  trace(7,start_trip).
  trace(8,end_trip).
}).
#pos(example_740@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_supervisor).
  trace(7,declaration_final_approved_by_director).
  trace(8,request_payment).
  trace(9,payment_handled).
}).
#pos(example_741@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,start_trip).
  trace(2,end_trip).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,send_reminder).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_742@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_supervisor).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_final_approved_by_supervisor).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_final_approved_by_supervisor).
  trace(9,request_payment).
  trace(10,payment_handled).
}).
#pos(example_743@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_pre_approver).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_approved_by_pre_approver).
  trace(7,declaration_final_approved_by_supervisor).
  trace(8,declaration_rejected_by_missing).
  trace(9,permit_rejected_by_missing).
}).
#pos(example_744@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_rejected_by_supervisor).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_approved_by_administration).
  trace(7,permit_rejected_by_budget_owner).
  trace(8,permit_rejected_by_employee).
  trace(9,permit_submitted_by_employee).
  trace(10,permit_approved_by_administration).
  trace(11,permit_rejected_by_budget_owner).
  trace(12,permit_rejected_by_employee).
  trace(13,permit_submitted_by_employee).
  trace(14,permit_approved_by_administration).
  trace(15,permit_approved_by_budget_owner).
  trace(16,permit_final_approved_by_supervisor).
  trace(17,send_reminder).
  trace(18,send_reminder).
  trace(19,declaration_submitted_by_employee).
  trace(20,declaration_approved_by_administration).
  trace(21,declaration_approved_by_budget_owner).
  trace(22,declaration_final_approved_by_supervisor).
  trace(23,request_payment).
  trace(24,payment_handled).
}).
#pos(example_745@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,end_trip).
  trace(4,permit_final_approved_by_supervisor).
  trace(5,send_reminder).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_final_approved_by_supervisor).
  trace(12,request_payment).
  trace(13,payment_handled).
}).
#pos(example_746@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_saved_by_employee).
}).
#pos(example_747@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,start_trip).
  trace(3,permit_final_approved_by_supervisor).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_rejected_by_administration).
  trace(10,declaration_rejected_by_employee).
  trace(11,declaration_submitted_by_employee).
  trace(12,declaration_rejected_by_administration).
  trace(13,declaration_rejected_by_employee).
  trace(14,declaration_submitted_by_employee).
  trace(15,declaration_approved_by_administration).
  trace(16,declaration_approved_by_budget_owner).
  trace(17,declaration_final_approved_by_supervisor).
  trace(18,request_payment).
  trace(19,payment_handled).
}).
#pos(example_748@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_supervisor).
  trace(3,permit_final_approved_by_director).
  trace(4,start_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,end_trip).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_749@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_approved_by_administration).
  trace(4,permit_approved_by_supervisor).
  trace(5,permit_final_approved_by_director).
  trace(6,declaration_submitted_by_employee).
  trace(7,declaration_rejected_by_administration).
  trace(8,declaration_rejected_by_employee).
  trace(9,declaration_submitted_by_employee).
  trace(10,declaration_approved_by_administration).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_750@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_approved_by_budget_owner).
  trace(3,permit_approved_by_supervisor).
  trace(4,permit_final_approved_by_director).
  trace(5,start_trip).
  trace(6,end_trip).
  trace(7,declaration_submitted_by_employee).
  trace(8,declaration_rejected_by_administration).
  trace(9,declaration_rejected_by_employee).
  trace(10,declaration_submitted_by_employee).
  trace(11,declaration_rejected_by_administration).
  trace(12,declaration_rejected_by_employee).
  trace(13,declaration_submitted_by_employee).
  trace(14,declaration_approved_by_administration).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
#pos(example_751@0, {recognized}, {}, {
  trace(0,permit_submitted_by_employee).
  trace(1,permit_approved_by_administration).
  trace(2,permit_final_approved_by_supervisor).
  trace(3,start_trip).
  trace(4,end_trip).
  trace(5,declaration_submitted_by_employee).
  trace(6,declaration_rejected_by_administration).
  trace(7,declaration_rejected_by_employee).
  trace(8,declaration_submitted_by_employee).
  trace(9,declaration_approved_by_administration).
  trace(10,declaration_approved_by_budget_owner).
  trace(11,declaration_approved_by_supervisor).
  trace(12,declaration_final_approved_by_director).
  trace(13,request_payment).
  trace(14,payment_handled).
}).
#pos(example_752@0, {recognized}, {}, {
  trace(0,start_trip).
  trace(1,end_trip).
  trace(2,permit_submitted_by_employee).
  trace(3,permit_rejected_by_administration).
  trace(4,permit_rejected_by_employee).
  trace(5,permit_submitted_by_employee).
  trace(6,permit_rejected_by_administration).
  trace(7,permit_rejected_by_employee).
  trace(8,permit_submitted_by_employee).
  trace(9,permit_approved_by_administration).
  trace(10,permit_approved_by_budget_owner).
  trace(11,permit_final_approved_by_supervisor).
  trace(12,declaration_submitted_by_employee).
  trace(13,declaration_approved_by_administration).
  trace(14,declaration_approved_by_budget_owner).
  trace(15,declaration_final_approved_by_supervisor).
  trace(16,request_payment).
  trace(17,payment_handled).
}).
